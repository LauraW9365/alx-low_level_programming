#include "main.h"

/**
 * _strlen - Returns the length of a string
 * @s: check the length of a string
 * Return: 0
 */

int _strlen(char *s)
{
	int b = 0; 

	for (; *s++;)
	       b++;

	return (b);
}
